# NPM Meta 配置文件
# 请将此文件复制为 .env 并修改配置

# Nginx Proxy Manager 服务器地址（必填）
# 格式：IP:端口 或 域名:端口
NPM_HOST=your-npm-server:81

# Flask 密钥（可选，用于 session 加密）
# 如不设置，每次启动会自动生成随机密钥
# SECRET_KEY=your-secret-key-here

# NPM 管理员账号（可选，用于后台健康检查）
# 如果配置了这两个参数，后台线程会自动登录 NPM 并检查所有端口的健康状态
# 如果不配置，只有在用户访问页面后才能开始健康检查
NPM_ADMIN_EMAIL=your-npm-admin@example.com
NPM_ADMIN_PASSWORD=your-npm-admin-password
